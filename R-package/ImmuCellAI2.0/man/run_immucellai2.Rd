% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/immucellai2.R
\name{run_immucellai2}
\alias{run_immucellai2}
\title{Run the standard ImmuCellAI 2.0 workflow}
\usage{
run_immucellai2(
  bulk,
  reference = NULL,
  marker.genes = NULL,
  hierarchy.mode = "tcell",
  add.unknown = FALSE,
  n.cores = 1,
  pseudo.depth = 1e+05,
  n.iter = 50,
  vb.tol = 1e-06,
  alpha.major = 10,
  alpha.sub = 5,
  alpha.state = 1,
  seed = 123,
  verbose = FALSE
)
}
\arguments{
\item{bulk}{A genes-by-samples numeric matrix or a path to a tab-delimited
matrix whose first column contains gene symbols.}

\item{reference}{Optional genes-by-cell-states matrix or reference file.
If omitted, the packaged 53-state atlas is used.}

\item{marker.genes}{Optional character vector or marker file. If omitted,
the packaged 5,510-gene panel is used.}

\item{hierarchy.mode}{One of `tcell`, `flat`, `hierarchical`, or
`hybrid`. The manuscript workflow uses `tcell`.}

\item{add.unknown}{Add an optional residual state for sensitivity analysis.
The manuscript workflow uses `FALSE`.}

\item{n.cores}{Number of samples processed concurrently.}

\item{pseudo.depth}{Pseudo-count depth used after within-sample scaling.}

\item{n.iter}{Maximum number of deterministic VB updates.}

\item{vb.tol}{Convergence tolerance for the maximum fraction change.}

\item{alpha.major, alpha.sub, alpha.state}{Hierarchical Dirichlet prior
concentrations.}

\item{seed}{Reproducibility seed.}

\item{verbose}{Print per-sample progress when running on one core.}
}
\value{
The result from [deconvolve_bulk_matrix()] plus a `settings` field.
}
\description{
This is the recommended user-facing entry point. It intersects bulk RNA-seq
TPM values with the 53-state reference atlas and the published 5,510-gene
marker panel, then runs deterministic variational Bayesian inference with
targeted hierarchical refinement of CD4 and CD8 T-cell states.
}
