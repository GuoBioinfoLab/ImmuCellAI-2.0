% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolve.R
\name{deconvolve_bulk_matrix}
\alias{deconvolve_bulk_matrix}
\title{Deconvolve a bulk expression matrix}
\usage{
deconvolve_bulk_matrix(
  bulk.mat,
  reference,
  hierarchy = NULL,
  hierarchy.mode = c("flat", "hierarchical", "hybrid", "tcell", "tcell_only"),
  inference.method = c("mcmc", "vb", "flat_vb"),
  add.unknown = FALSE,
  unknown.state = "UNKNOWN",
  unknown.mode = c("uniform", "mean"),
  pseudo.depth = 1e+05,
  n.iter = 150,
  burn.in = 50,
  alpha.major = 10,
  alpha.sub = 5,
  alpha.state = 1,
  vb.tol = 1e-07,
  prior.theta = NULL,
  use.unknown.update = add.unknown,
  adaptive.params = list(),
  n.cores = 1,
  seed = 123,
  verbose = FALSE
)
}
\arguments{
\item{bulk.mat}{Gene-by-sample non-negative expression matrix.}

\item{reference}{Gene-by-state non-negative reference matrix.}

\item{hierarchy}{Optional table with state_name, subtype, and major_lineage.}

\item{hierarchy.mode}{One of flat, hierarchical, hybrid, or tcell. The legacy value tcell_only is accepted as an alias.}

\item{inference.method}{One of vb, flat_vb, or the legacy mcmc path.}

\item{add.unknown}{Add an optional latent residual state.}

\item{unknown.state}{Name assigned to the residual state.}

\item{unknown.mode}{Initial residual profile: uniform or reference mean.}

\item{pseudo.depth}{Total pseudo-count mass used for each sample.}

\item{n.iter}{Maximum number of VB updates or legacy MCMC iterations.}

\item{burn.in}{Used by the legacy MCMC path and optional residual adaptation;
it does not define posterior averaging for ordinary VB runs.}

\item{alpha.major, alpha.sub, alpha.state}{Dirichlet prior concentrations.}

\item{vb.tol}{Maximum absolute fraction change used as the VB stopping rule.}

\item{prior.theta}{Optional named state-level prior probability vector.}

\item{use.unknown.update}{Adapt the UNKNOWN profile from residual expression.}

\item{adaptive.params}{Optional list overriding residual-update parameters.}

\item{n.cores}{Number of samples processed concurrently.}

\item{seed}{Reproducibility seed used for workers and legacy sampling.}

\item{verbose}{Print per-sample or per-iteration progress.}
}
\value{
A list containing sample-by-state, subtype, and major fraction
  matrices, the hierarchy, genes used, and per-sample final references.
}
\description{
Lower-level engine supporting deterministic hierarchical or flat VB and a
legacy MCMC compatibility path. New analyses should normally use
run_immucellai2().
}
